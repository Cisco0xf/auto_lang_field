// ignore_for_file: constant_identifier_names

// Supported Language must have at least one item

const String SUPPRTEDLANGUAGES =
    "supportedLanguages List<LanguageCode> must have at least one item";

// Length of the TextProperties more than expected

const String LENGTHERROR =
    "Length of the textPropertiesPerLanguage > length of the supportedLanguages ";

// When LanguageCode key not exist in the [supportedLanguages]

const String UNSUPPORTEDLANGUAGECODE =
    "Unsupported LangugeCode are provided for TextProperties";
